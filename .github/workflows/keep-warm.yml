name: Wake Database

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping DB health endpoint
        run: curl -fsS --retry 3 --retry-delay 10 --max-time 60 "${{ vars.AZURE_WEBAPP_URL }}/api/health/db"
